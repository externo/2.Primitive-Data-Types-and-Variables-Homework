using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace VariableInHexadecimalFormat
{
    class Program
    {
        static void Main(string[] args)
        {
            //assign hex value as int32
            Int32 hexV = 0xFE;

            //print hex value in integer format
            Console.WriteLine("Hex value 0xFE is printed as integer {0}", hexV);
        }
    }
}
